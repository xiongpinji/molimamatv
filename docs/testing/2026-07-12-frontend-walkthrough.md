# 2026-07-12 前端逐页测试清单

## 测试范围

- 品牌 LOGO：登录、注册、侧栏、浏览器图标。
- 公共页面：登录、注册。
- 登录后页面：工作台、项目、无限画布、生成设置、背景音乐、账号、发布、API 密钥、设置、提示词库。
- 核心交互：导航、表单、弹窗、提示词选择、画布入口。
- 模型连通：推理文字、文生图、视频生成各执行一次最小测试。

## 验收标准

- 页面无阻断性控制台错误和资源 404。
- 所有导航入口可到达，关键按钮可交互。
- LOGO 在桌面及移动视口不变形、不溢出。
- 三组测试模型各得到明确成功结果，或记录真实服务端错误。
- 每个修复点复测通过，前端生产构建通过。

## 安全边界

- 测试密钥只在本机运行时输入，不写入仓库、截图、日志或报告。

## 本轮结果

- LOGO 已接入 favicon、登录/注册页和主侧栏，并完成桌面与 390x844 移动视口检查。
- 登录、注册、控制台及 9 个登录后页面均已用真实浏览器访问。
- 修复 `/api-keys` 直达被开发代理拦截的问题。
- 修复提示词库重定向 CORS、超限分页参数和重复 `.data` 解包问题。
- 修复视频任务页与创建弹窗的 Element Plus 单选项弃用警告。
- 三组测试模型配置已通过本机 API 保存，UI 正确显示且密钥遮罩。
- 上游 `/models` 连通：文字模型精确存在；图片、视频 TXT 名称与服务返回 ID 不一致，真实生成尚不能声明通过。
- 前端测试现状：17 个文件中 14 个通过；历史认证/Login 测试装配及 CanvasEditor 套件仍有 3 个文件失败。