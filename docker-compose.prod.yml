version: '3.8'

# 茉莉妈妈短剧工作台 - 生产环境完整部署
# 包含所有服务:前端、后端、Worker、数据库、缓存、存储

services:
  # ==========================================
  # 基础设施服务
  # ==========================================

  # PostgreSQL 数据库
  postgres:
    image: postgres:15-alpine
    container_name: aicon-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-aicg_platform}
      POSTGRES_USER: ${POSTGRES_USER:-aicg_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-aicg_password}
      POSTGRES_INITDB_ARGS: "--encoding=UTF8 --locale=C"
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - aicon-network
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-aicg_user} -d ${POSTGRES_DB:-aicg_platform}" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis 缓存和消息队列
  redis:
    image: redis:7-alpine
    container_name: aicon-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-redis_password}
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data
    networks:
      - aicon-network
    healthcheck:
      test: [ "CMD", "redis-cli", "--raw", "incr", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5

  # MinIO 对象存储
  minio:
    image: minio/minio:latest
    container_name: aicon-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin123}
    ports:
      - "${MINIO_API_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    volumes:
      - minio_data:/data
    networks:
      - aicon-network
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:9000/minio/health/live" ]
      interval: 30s
      timeout: 10s
      retries: 3

  # ==========================================
  # 应用服务
  # ==========================================

  # 后端 API 服务
  backend:
    image: qingshui869413421/aicon-backend:latest
    container_name: aicon-backend
    restart: unless-stopped
    environment:
      # 数据库配置
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-aicg_user}:${POSTGRES_PASSWORD:-aicg_password}@postgres:5432/${POSTGRES_DB:-aicg_platform}

      # Redis配置
      REDIS_URL: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0
      CELERY_BROKER_URL: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0
      CELERY_RESULT_BACKEND: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0

      # MinIO配置
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin123}
      MINIO_BUCKET: ${MINIO_BUCKET:-aicg-platform}
      MINIO_SECURE: "false"

      # 应用配置
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:-your-secret-key-change-in-production}
      API_KEY_ENCRYPTION_KEY: ${API_KEY_ENCRYPTION_KEY:-zWpJNO3LF7RpaOvkpOG-edszCEQVWe24dcent10bSrY=}
      DEBUG: ${DEBUG:-false}
      ENVIRONMENT: production
      MINIO_PUBLIC_URL: ${MINIO_PUBLIC_URL:-localhost:9000}

    ports:
      - "${BACKEND_PORT:-8000}:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - aicon-network
    volumes:
      - ./backend/src:/app/src # 挂载源代码，支持热重载
      - ./backend/logs:/app/logs
    command: >
      sh -c "
        alembic upgrade head &&
        uvicorn src.main:app --host 0.0.0.0 --port 8000
      "

  # Celery Worker - 异步任务处理
  celery-worker:
    image: qingshui869413421/aicon-backend:latest
    container_name: aicon-celery-worker
    restart: unless-stopped
    environment:
      # 使用与backend相同的环境变量
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-aicg_user}:${POSTGRES_PASSWORD:-aicg_password}@postgres:5432/${POSTGRES_DB:-aicg_platform}
      REDIS_URL: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0
      CELERY_BROKER_URL: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0
      CELERY_RESULT_BACKEND: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin123}
      MINIO_BUCKET: ${MINIO_BUCKET:-aicg-platform}
      MINIO_SECURE: "false"
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:-your-secret-key-change-in-production}
      API_KEY_ENCRYPTION_KEY: ${API_KEY_ENCRYPTION_KEY:-zWpJNO3LF7RpaOvkpOG-edszCEQVWe24dcent10bSrY=}
      MINIO_PUBLIC_URL: ${MINIO_PUBLIC_URL:-localhost:9000}
    depends_on:
      - backend
      - redis
    networks:
      - aicon-network
    volumes:
      - ./backend/src:/app/src # 挂载源代码，支持热重载
      - ./backend/logs:/app/logs
    command: celery -A src.tasks.app worker --loglevel=info --concurrency=4

  # Celery Beat - 定时任务调度
  celery-beat:
    image: qingshui869413421/aicon-backend:latest
    container_name: aicon-celery-beat
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-aicg_user}:${POSTGRES_PASSWORD:-aicg_password}@postgres:5432/${POSTGRES_DB:-aicg_platform}
      REDIS_URL: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0
      CELERY_BROKER_URL: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0
      CELERY_RESULT_BACKEND: redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/0
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:-your-secret-key-change-in-production}
      API_KEY_ENCRYPTION_KEY: ${API_KEY_ENCRYPTION_KEY:-zWpJNO3LF7RpaOvkpOG-edszCEQVWe24dcent10bSrY=}
      MINIO_PUBLIC_URL: ${MINIO_PUBLIC_URL:-localhost:9000}
    depends_on:
      - backend
      - redis
    networks:
      - aicon-network
    volumes:
      - ./backend/src:/app/src # 挂载源代码，支持热重载
      - ./backend/logs:/app/logs
    command: celery -A src.tasks.app beat --loglevel=info

  # 前端服务
  frontend:
    image: qingshui869413421/aicon-frontend:latest
    container_name: aicon-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-80}:80"
    depends_on:
      - backend
    networks:
      - aicon-network

# ==========================================
# 网络配置
# ==========================================
networks:
  aicon-network:
    driver: bridge
    ipam:
      config:
        - subnet: 192.168.223.0/24

# ==========================================
# 数据卷配置
# ==========================================
volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  minio_data:
    driver: local
